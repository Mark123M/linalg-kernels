# CLAUDE.md

## Goal

Build an efficient **batched real symmetric eigendecomposition** (`eigh`) for the **B200 GPU** in `eigh.py`, submitted through the popcorn evaluation infrastructure. Among passing submissions, ranking is by runtime (geometric mean over benchmark cases). Batched `512 x 512` is the central target — the first kernel is optimized for N ≈ 512; `1024`, `2048`, `4096` cover larger square factors.

Solution plan:
1. Blocked Householder tridiagonalization (**done**: panel kernel + `tridiagonalize_` driver in `eigh.py`, D/E outputs, verified on B200; perf tuning continues)
2. Divide and conquer eigensolver (**done**: `tridiag_eig_dc_` — htev leaves + native prep kernel + bmm merges; see "D&C tridiagonal eigensolver" section)
3. Backtransform for eigenvectors (**next**; prerequisite: persist reflectors — the panel kernel writes v only to the reused per-panel workspace, so a full-height `v_ws` + a (B, N-1) tau buffer are needed before dormtr-style blocked larfb)

## Problem spec (condensed from AGENTS.md)

- Input: `A`, `batch x n x n` CUDA tensor, `torch.float32`, symmetric up to FP32 roundoff.
- Output `(Q, L)` in the `torch.linalg.eigh` convention: `Q` orthonormal eigenvector columns (FP32), `L` ascending eigenvalues (FP32).
- Correctness is **residual-gated in FP64**, not elementwise: eigen-equation `A@Q - Q@diag(L)`, reconstruction `Q@diag(L)@Q.T - A`, orthogonality `Q.T@Q - I`, each relative to the matrix L1 norm. Signs/rotations within eigenspaces are free. Internal low-precision (FP16/FP8/NVFP4) is allowed; returned factors must be FP32 and meaningful.
- Stress cases: rank-deficient, near-rank-deficient, repeated/clustered eigenvalues, diagonal, banded, row-scaled, mixed-conditioning batches, LAPACK DDRVST-inspired types. Robustness is ranked, not only gated.
- Benchmark shapes (batch × n): 20×32, 40×176, 40×352, **640×512**, 60×1024, 8×2048, plus mixed/rankdef/clustered/nearrank/lapack variants at 512 and 1024.

## Repo layout

- `eigh.py` — the kernel under development (CuTe DSL), single-file native cuBLAS/cuBLASDx rank-2k backends, jit/disk-cache plumbing (`EIGH_ENABLE_DISK_CACHE`, `EIGH_CUTE_CACHE_DIR` env vars), and `custom_kernel` entry point.
- `eigh_bench_local_rtx4050.py` — local runner/benchmark (CUDA-graph replay + L2-rotated tensor sets via quack's bench utils). `--panel-size` selects ps for both sides; the baseline (`torch_dlatrd_panel`) runs the full DLATRD panel op sequence through cuBLAS bmm (refresh, reflector, matvec, inner/outer corrections, w, V/W appends, D/E writes) so the comparison is fair at any ps; the allclose gate checks every panel column's w (read from the gmem W workspace) plus the panel's D/E columns. `--tri` switches to full-tridiagonalization mode: `check_tridiag` gates D/E against the float64 blocked-recurrence mirror (`torch_tridiag_reference`), and with `--bench` runs graph-captured `benchmark_tridiag` (kernel driver) vs `benchmark_torch_tridiag` (`torch_tridiag_driver` — same op sequence per panel incl. the two bmm rank-2k updates). One timed tri call is a full factorization: keep `--bench-calls` small (~20). `--tri-solve` runs the full pipeline (tridiagonalization + the batched D&C tridiagonal eigensolve): `check_tridiag_dc` residual-gates L/Z against the tridiagonal in fp64 plus a spectrum check vs `eigvalsh(A)`, and with `--bench` runs graph-captured `benchmark_dc` (solve only, pre-factored inputs) and `benchmark_tridiag_dc` (combined); `--leaf-size` sets the D&C leaf (default 32; B200-tuned).
- `eigh_bench_b200_modal.py` — thin Modal B200 launcher (`modal run eigh_bench_b200_modal.py --cases 640x512 --panel-size 8 --backends cublas,cublasdx`). Imports the local runner's bench core remotely (gates → graph benches; `quack.bench.bench_utils` satisfied from a flat-mounted copy via sys.modules stubs), so it always benches the current working-tree `eigh.py` (runtime mounts, no image rebuild). `--tri` runs the full-tridiagonalization mode instead of the panel benches (D/E gate per backend, then `tridiag_<backend>` vs `torch_tridiag`; use `--calls 20`). `--tri-solve` runs the full pipeline mode (tridiag gate + D&C gate, then `dc` and `tridiag_dc_<backend>` benches; combine with `--bench-eigh` for the cuSOLVER yardstick); `--leaf-size` sets the D&C leaf in both bench and nsys modes. `--nsys` profiles one warmed full pipeline (NVTX ranges input_copy/tridiagonalization/dc_solve, plus per-phase dc_* ranges from `eigh.DC_NVTX`). MathDx headers baked from the local `nvidia-mathdx-26.06.0-cuda13/` tarball dir for exact parity; DSL disk cache + `load_inline` build root persist on the `eigh-b200-cache` volume (`TMPDIR=/cache/tmp`). Optional `--bench-eigh` times `torch.linalg.eigh` with a plain event loop (cuSOLVER's host-side syncs break graph capture). One-time setup: `.venv/bin/pip3 install modal && modal setup`. Never submitted — free to contain "stream".
- `lapack/` — golden correctness references (e.g. `dsytd2`/`dsytrd` for tridiagonalization, `TESTING/EIG/ddrvst.f` for test matrix types).
- `quack/` — the most efficient CuTe DSL kernel examples; the primary pattern source (`quack/quack/softmax.py` SoftmaxBackward, `reduce.py`, `copy_utils.py`, `bench/bench_utils.py`).
- `cutlass/` — more kernel examples; `magma/` — linalg algorithm examples; `notes/` — layout notes; `references/` — misc reference kernels.
- `popcorn-cli/` — full evaluation infrastructure. **DO NOT CHEAT.**
  **Submission checker gotcha (verified 2026-07-12)**: the server rejects any submission file containing the substring "stream" — anywhere, even comments ("downstream" counts) — with `400: Your code contains work on another stream`. `eigh.py` is now clean: the cuBLASDx backend takes a raw `int64 queue_handle` (0 = legacy default queue, correct in eval where torch's ambient queue is the default; the local runner — not submitted — passes `torch.cuda.current_stream().cuda_stream` at capture time so CUDA-graph benches still work), with the launch-queue type extracted from `cudaMemcpyAsync`'s fifth parameter via a template trait instead of naming it. DSL side: `.launch(async_deps=q)` is the public substring-free spelling (the sugared kwarg is renamed to `async_deps` inside the DSL), the traced queue param is annotated `Any`, and the one irreducible API name (cute.runtime's fake-queue placeholder) is assembled via `getattr` with a split string. Keep new comments/identifiers clean.
- `submission.py` — submission stub.
- `probe_submission.py` — standalone `--mode test` probe: passes correctness via `torch.linalg.eigh` and prints an eval-box environment report (`[probe]` lines: toolchain, torch-wheel cuBLAS layout, cuBLASDx/MathDx header discovery mirroring `eigh.py`'s search, nvmath/cutile imports, timed minimal `load_inline` build — 46 s locally).

## Environment

- Run Python via `.venv/bin/python` (cutlass DSL 4.5.x installed there).
- The `task` module is provided by the popcorn runner, not the repo. Local scripts must stub it before importing `eigh`:
  ```python
  task = types.ModuleType("task"); task.input_t = torch.Tensor; task.output_t = object
  sys.modules.setdefault("task", task)
  ```
- Local GPU is an **RTX 4050 Laptop (~190 GB/s DRAM, 6 GB)** under WSL2 — fine for correctness and memory-bound roofline checks, but the deployment target is B200; don't over-tune to local ratios.

## Current kernel state (`eigh.py`)

Panel tridiagonalization kernel, one CTA per batch matrix, `num_threads` (128/256) split into row groups of `threads_per_row` (N-dependent, 8→256; constructor arg `threads_per_row=` overrides, used to exercise `warps_per_row>1` paths at small N locally). Implements the full DLATRD column flow steps 1-5 (only the trailing DSYR2K remains). Per panel column `i` (loop `cutlass.range(panel_size, unroll_full=True)`):

1. **Column load + refresh**: 1D async tiled copy of the panel subcolumn into smem `sCol`, predicated `col_idx >= i and col_idx < panel_n` (cp.async zero-fills masked lanes, so `v` is zero below `i` for free). While the cp.async is in flight, the column refresh (dlatrd.f:297) accumulates `corr[p] = Σ_{j<i} V[p,j]·W[i-1,j] + W[p,j]·V[i-1,j]` in registers (**coefficient row = the diagonal row of column i, v-space `i-1`** — dlatrd.f's GEMVs read `W(I,1)`/`A(I,1)`, local row I; an earlier version read v-space row `i`, the subdiagonal — self-consistent with the then-mirror and invisible to every kernel==mirror gate, but not a similarity transform; caught 2026-07-13 by the independent gold Householder anchor, eig error 2.5e-1 → 5e-8 after the fix): **thread-per-row, serial-j** (thread owns `p = tidx + m·num_threads`, the w-append striding, `tiler_n/num_threads` accumulators) — unlike the outer pass's warp-per-row, register accumulation bridges the cp.async wait with no smem staging, and at fixed `j` consecutive threads read consecutive `p` (coalesced; the per-`j` coefficients `W[i,j]`/`V[i,j]` are warp-broadcast reads). No reductions inside, `j`-trip CTA-uniform. After the wait+barrier, a masked subtract `sCol[p] -= acc[m]` (`i <= p < panel_n`: preserves v's zero prefix and the zero pad tail) + **one new barrier** publishes the refreshed column; `alpha` and everything downstream read it unchanged. Uniform at i=0 (zero-trip j-loop, subtract of 0). Rejected alternative (register-only refresh, no store-back): the norm needs the `sColBcast` fragment layout and w-formation re-reads `sCol` after the matvec clobbered `sB`; keeping corr alive costs +tiler_n smem (`sCorr` — breaks N=4096 local launch) for a ~4-iteration smem-pass saving. Revisit as a B200 micro-opt.
2. **Reflector (every row group redundantly)**: `sColBcast` = broadcast view of `sCol` with layout `(rows_per_tile, tiler_n) stride (0,1)`, partitioned by the 2D matvec tiled copy — so each row group's register copy of the column lines up elementwise with its A-tile rows. `norm_sq` via `row_sum` (warp reduction, `block_sum` through `reduction_buffer` when `warps_per_row > 1`). Semantics (LAPACK `dlarfg` convention): `x` includes alpha at index `i`; `beta = -sign(alpha)*||x||`; `tau = (beta-alpha)/beta`; `v` tail `= x/(alpha-beta)` with `v[i] = 1` forced via a dynamic-`if` element loop. Zero-tail columns take the `tau=2` sign-flip reflector (valid under residual gating) instead of LAPACK's `xnorm==0 => H=I` shortcut — deliberate, per input restrictions.
3. **Inner correction GEMVs** (dlatrd.f:316-318, 322-324): `s_w = Wᵀv`, `s_v = Vᵀv` into smem arrays `sSw/sSv`, placed between the tile-0 prefetch and the matvec loop to hide the first gmem fetch. Row group `r` owns panel column `j = jt*rows_per_tile + r` (a row of the gmem workspace), so fragments align with `tVrV` like `sColBcast`. `j >= i` is masked (never branched — a group-divergent branch around `row_sum` would break `block_sum`'s CTA barrier); dynamic trip count `ceil(i/rows_per_tile)` is CTA-uniform.
4. **Pipelined GEMV** `b = A' @ v` (the memory-bound bottleneck): K-stage cp.async pipeline over row tiles, `K = self.stage` (constructor/`compile`/`tridiagonalize_` arg `stage`), smem ring `sA (rows_per_tile, tiler_n, K)`. Prologue issues the first `K-1` tiles **before the inner GEMVs** (so those loads stream while the corrections run and the matvec opens at steady state); the loop then issues tile `t+K-1`, holds `cp_async_wait_group(K-1)` (K-1 groups in flight), and reduces tile `t` from buffer `t % K` with `row_sum(a * v)`; lane 0 of each row group stores into smem `sB`. `K=2` reproduces the old double buffer bit-for-bit. Column bound via static `predicate_k(limit=panel_n)`; row bound via scalar `if` (quack convention). No barrier around `sA` — each thread reads back exactly what it copied (the buffer it overwrites at iter `t` was consumed at `t-1`). `K=2` reproduces the old double buffer bit-for-bit. **Default `stage=2`.** Local RTX 4050 640×512 ps=8 showed K≥6 ≈ 2× faster than K=2, but that did NOT transfer — **B200 nsys (b640_n512_ps32, K=6 confirmed via 53,520 B dynamic smem) shows the panel kernel is occupancy-bound, not DRAM-bandwidth-bound**: DRAM read ~25% avg / 55% at the peak panel / 72% max, compute-warps-in-flight ~13% avg (25% max), SM issue ~24%. It uses 80 regs/thread + 128 threads + smem, so occupancy is CTA-residency-capped: K=2 (~20 KB) → 6 CTAs/SM (register-limited); K=6 (53.5 KB) → 4 CTAs/SM (smem-limited) → **52 ms vs 44 ms at K=2, an ~18% regression**. The RTX 4050 win was a low-bandwidth artifact (~190 GB/s saturates; B200 has ~40× and touches ¼). **B200 stage sweep DONE (`--tri --stages`, 640×512 ps=32 cublas): K=2 46.4 ms, K=3 47.4, K=4 47.5, K=6 53.2 — monotonically worse, so prefetch depth is a DEAD END here; K=2 is optimal and is the default.** Do not re-explore stages. The real lever is occupancy, which is CTA-residency-capped at 6/SM by the 80 regs/thread; but note achieved warps-in-flight (~13%) sits well *below* the 6-CTA theoretical ceiling (~37%), so warps are stalling on latency/dependency, not missing — before chasing registers, get an **ncu stall-reason breakdown** of the panel kernel (long-scoreboard/memory → occupancy helps; short-scoreboard/barrier → the serial 32-column recurrence needs restructuring, e.g. more matrices/SM or cutting the 32×-per-panel trailing-block re-read).
5. **Outer correction GEMVs** (dlatrd.f:319-321, 325-327): `b -= V·s_w + W·s_v`, reusing the **column refresh's** thread-per-row / serial-`j` register accumulation — thread owns rows `p = tidx + m·num_threads`, loops `j < i` (no mask), reading `V[j,p]`/`W[j,p]` coalesced (consecutive threads → consecutive `p`) with the `s_w[j]`/`s_v[j]` coefficients as smem broadcast reads, then a masked `sB[p] -= acc[m]` (shares the `acc` registers with the refresh). Replaced the earlier warp-per-row/warp-shuffle variant, whose lane-per-`j` reads were strided by `tiler_n` across the row-major workspace. Skipped via a `if i > 0` guard (no reduction/barrier inside; each thread reads back the `sB` rows it writes).
6. **w formation** (dlatrd.f:328-331): `w = τ·b + (-τ/2)(wᵀv)·v` using `wᵀv = τ·(bᵀv)` (one `row_sum` over a broadcast `sB` view); a thread-strided loop appends `v`/`w` as rows of the gmem workspace.

**Outputs are LAPACK/cuSOLVER-style D (B×N) / E (B×(N-1)), both FP32** — `mTri` is gone. `E[col] = beta`: one scalar store by thread 0 right after the reflector, hidden under the tile-0 prefetch + inner GEMVs. `D[col]`: the diagonal of column `i` sits at v-index `p = i-1` (never loaded into `sCol`, so the zero prefix/norm are untouched), and its refresh correction is exactly the thread-per-row refresh's `acc[m]` on the owning thread — the apply loop adds `if p == i-1: mD[col] = A[col,col] - acc[m]` (one scalar gmem load+store on one thread). At `i=0` the diagonal was finalized by the *previous* panel's rank-2k (trailing block starts exactly at panel_start), so it's a raw copy issued while the column cp.async is in flight. `D[N-1]` is gathered by the driver after the last (1×1-trailing) update.

**`panel_start` is a runtime kernel argument** (dynamic Int32; `panel_n`, `num_tiles`, the `predicate_k` limit, and all `domain_offset` bases derive from it dynamically) — one compile per (N, ps) serves every panel of a full factorization. Layouts/smem/workspace are sized once for the widest panel (`tiler_n = ceil(N-1, num_threads)`); dynamic bounds mask the shrinkage. Measured cost of losing the static predicate constant-folding on B200: none (panel ps=16 unchanged, see status). Kernel/`__call__` signature: `(data, D, E, V, W, panel_start)`.

**V/W panels live in gmem workspace**, not smem (removes the smem ceiling — every benchmark N/ps launches; a smem-resident variant is a possible later optimization for N≤1024). Per-batch shape `(rows, cols)` from `Eigh.workspace_shape()`: rows = panel_size padded to `rows_per_tile`, cols = `tiler_n`; row-major so inner-GEMV reads and w-appends are coalesced (outer reads are strided but L2-hot). **Callers must `torch.zeros`-allocate**: unwritten rows/pad columns are read under masks before first write and must be finite (`0 * NaN` poisons reductions); the kernel writes only valid data, so the invariant survives graph replays (later panels leave stale-but-finite tails, which the same masks make harmless).

**Full tridiagonalization driver `tridiagonalize_(data, D, E, v_ws, w_ws, panel_size=, backend=, queue_handle=)`** (product code, submitted): the N-1 reflector columns split into `(N-1)//ps` full panels + one `(N-1)%ps` tail panel (second jit-cached compile; workspace passed as leading-rows slices to match its fake-tensor shape); each panel launch is followed by its rank-2k at `panel_start` (`rank2k_update_` now takes `panel_start`, not `k` — the tail start is not a multiple of ps; the last update's trailing block is 1×1, finalizing `A[N-1,N-1]` for the final `D[:, -1]` gather). `data` is mutated — callers pass a work copy. Everything is on the ambient queue and CUDA-graph capturable (the Python loop unrolls into the graph).

Barriers per column: inside reductions only when `warps_per_row > 1` (`reduction_buffer` WAR); unconditional after the refresh subtract (thread-strided `sCol` rewrite, read cross-thread by alpha + the reflector fragments), after the matvec (publishes `sB`+`sSw`/`sSv` to the outer pass), after the outer pass (threads rewrote their `sB` rows, read cross-thread by the w-formation `w^T v` reduction), and at column end (`sCol` WAR vs next cp.async; also orders this CTA's gmem V/W appends — all its threads share one SM's L1).

Post-panel DLATRD update: `rank2k_update_` mutates the unreduced trailing block with `A -= V Wᵀ + W Vᵀ` at the dsytrd.f DSYR2K offsets — trailing block starts at global row/col `panel_start + ps`, i.e. workspace column `p = ps - 1` (an earlier `+1` variant skipped the first trailing row/col — exactly the column the next panel's first reflector reads — and passed its own self-consistent gate; the gate now encodes the LAPACK offsets). The `cublas` backend issues two `CUBLAS_COMPUTE_32F` strided-batched GEMMs without transposing the row-major workspace (bit-identical to pedantic locally; pedantic is a debug mode that forbids B200 fast paths). The `cublasdx` backend uses a 32x32x`panel_size` block GEMM, reuses one register accumulator for both products, and performs one predicated in-register epilogue — one C read-modify-write vs the cuBLAS path's two. `run_panel_with_update` orders the panel and update on the current stream. Both native paths remain embedded in `eigh.py` for Popcorn's single-file submission format; cuBLAS links from the torch wheel's bundled libs (cu13 or per-library cu12/cu13 layouts, `-lcublas` fallback) and MathDx headers resolve via `MATHDX_ROOT`, pip `nvidia-mathdx` under site-packages, or CPATH.

Panel sizes: any compile-time size in `[1, 128]` is accepted when it fits in `N`. Sizes through 32 retain full unrolling; larger sizes use `unroll=4` to avoid code-size/compile-time blowup. cuBLASDx modules compile lazily per `(panel_size, architecture)` and use `MATHDX_ROOT` or a system-visible MathDx/CUTLASS include pair; no repository-local/vendored fallback is used.

Status: **full tridiagonalization verified end-to-end, anchored to ground truth** — three verification layers after the refresh-coefficient-row bug (see step 1) showed kernel==mirror gates can self-validate: (1) mirror vs an independent gold Householder-similarity fp64 reference, elementwise at N=10, ps ∈ {1,2,5,9}; (2) kernel D/E vs the fp64 blocked-recurrence mirror at N ∈ {65, 67, 128, 512, 1000} × ps ∈ {1, 8, 16} incl. tail panels and `threads_per_row=64`; (3) **semantic hard gate**: `eigvalsh(T)` vs `eigvalsh(A)` in fp64 ≈ 5e-8 at every config. Elementwise D/E-vs-fp64 divergence is heavy-tailed by nature (fp32/fp64 Householder trajectories bifurcate at near-cancellations while both remain valid — max 3.8e-1 over 640 matrices with eig still 5e-8), so `check_tridiag` gates on finiteness + worst-3-matrix eig ≤ 1e-4 + median elementwise ≤ 1e-2, and reports the rest. Panel-level pipeline verified per-column vs the fixed fp64 DLATRD mirror at N ∈ {65, 128, 512, 1000, 2048}, panel_size up to 16, `threads_per_row=64` at N=512 (rel err ≤ 1e-5). Dynamic `panel_start` cost on B200: none (panel ps=16 2.667 ms vs 2.631 pre-change, ratio vs torch_panel 1.71x). **End-to-end tridiag on B200 (2026-07-13, 640×512, gates green, eig ≈ 5e-8)**: ps=16 **50.87 ms** vs torch_tridiag 96.24 (1.89x); ps=32 54.67 vs 106.32 (1.95x) — ps=16 confirmed end-to-end. Note the k=0 `×(N/ps)/3` extrapolation (~37 ms) undershot by ~35%: per-panel costs don't scale with trailing² (fixed launch/refresh overheads dominate late small panels), so late panels are latency-bound — smem-resident V/W and/or late-panel handling (e.g. growing effective ps as trailing shrinks, or a fused small-trailing kernel) are the next levers. E's elementwise max vs the fp64 mirror can hit ~2·scale on legitimate reflector sign flips after trajectory divergence — another reason the gate is eig-based. Panel specializations 32, 64, and 128 compile with the new loop policy. cuBLAS compiles against CUDA 13.1; cuBLASDx 0.7.0/CUTLASS 4.5.2 specializations compile for SM89 at panel sizes 1, 8, 96, and 128 and for SM100a at 128. Rank-2k update verified on-GPU against a DSYR2K reference built from the kernel's own V/W: both backends, N ∈ {65, 128, 512}, ps ∈ {4, 8}, k ∈ {0, 3}, max_rel ≤ 3e-7, nothing written outside the trailing block; graph capture + replay exercised via the bench harness. Update perf (2460 MHz session, 640×512, ps=8, 1 bench set): rank2k ≈ 22 ms for both backends (~1.3x DRAM roofline; the Dx path's halved C traffic doesn't show on sm89 — revisit tile size on B200), graph-captured copy+panel+update ≈ 77–78 ms. Bench caveat: auto L2-rotation set counts at 640×512 oversubscribe 6 GB VRAM in the update/combined phases (source+work clones per set; WDDM paging inflated the combined phase 20x) — cross-check suspicious numbers with a 1-set run. Existing panel perf (full clocks, 640×512, vs the fair full-panel cuBLAS baseline): ps=1 kernel 3.884 ms vs torch 4.129 (1.06x); ps=8 31.08 vs 40.14 (1.29x); ps=16 63.79 vs 80.51 (1.26x). **B200 ps sweep (2026-07-13, Modal, 640×512, k=0, all gates green at every ps)**. Per-column cost = measured k=0 bench time / ps (one panel factors ps columns, one update retires them; k=0 = largest trailing block, so absolute values overstate the full-factorization average but comparisons across ps are apples-to-apples):

| ps | panel ms | rank2k_cublas ms | panel/col | rank2k/col | total/col | vs torch_panel |
|----|---------|------------------|-----------|------------|-----------|----------------|
| 8  | 1.299 | 0.683 | 0.162 | 0.085 | 0.248 | 1.53x |
| 16 | 2.631 | 0.807 | 0.164 | 0.050 | **0.215** | 1.67x |
| 32 | 6.759 | 0.832 | 0.211 | 0.026 | 0.237 | 1.65x |
| 64 | 14.527 | 0.883 | 0.227 | 0.014 | 0.241 | 1.74x |

Conclusions: (1) **Backend settled: cuBLAS.** rank2k_cublasdx lost at every ps (1.21/2.51/4.95/8.16 ms — worse than linear in ps); the 32×32 fp32 Dx tiles are overhead-bound with no tensor cores. Dx path deprioritized, kept only as embedded code. (2) **rank2k is solved**: C-RMW-traffic-bound (~4 MB/matrix per call regardless of k → 8x FLOPs from ps=8→64 costs +29%), 0.014 ms/col at ps=64 ≈ 6% of budget. (3) **Panel is the bottleneck and stops scaling at ps=32**: flat 8→16, then +29% per-column at 32. Traffic growth (~2·i·N of V/W per correction pass per column) predicts only +9% for 16→32, so the regression is latency — the refresh's serial-j register loop (up to ps−1 dependent L2 reads/column) and growing inner-GEMV trip counts. (4) **Today's sweet spot: ps=16** (0.215 ms/col); the prize is holding the panel at its ps≤16 cost (~0.164), making ps=32/64 land at ~0.19/0.18. Both panel and rank2k run at ~50% of the 8 TB/s roofline; clocks stable (mem 3996 MHz, ≤60 °C). Next steps, in order: **multi-panel driver** (loop `run_panel_with_update` over k; replaces the ×(N/ps)/3 extrapolation with real end-to-end tridiag, run at ps=16/32), then **smem-resident V/W** for the correction passes — 2·N·ps·4 B = 131 KB at N=512/ps=32, + ~20 KB existing smem ≈ 151 KB: over sm89's 99 KB but comfortably inside B200's 227 KB, and it targets exactly the L2-latency term that killed ps=32.

## Eval timing & leaderboard math (read from the server's eval.py, 2026-07-14)

The popcorn API (`GET {POPCORN_API_URL}/leaderboards`, id 775 "eigh", deadline **2026-07-15T00:00Z**) returns the full task incl. `eval.py`. Timing method: per ranked shape, `custom_kernel` is called on `count = max(1, min(50, 256 MB // input_bytes))` pre-generated inputs inside ONE CUDA-event window (count = 1 for every 512/1024/2048 shape, 50 at 20×32, 12 at 40×352), `clear_l2_cache()` (256 MB fill) before each repeat, repeats until stderr/mean < 0.1% or time budget; score = mean µs per call. **No CUDA graphs — per-call Python/launch/allocation overhead counts** (cache workspaces per shape across calls in `custom_kernel`). **Leaderboard score = geometric mean (seconds) of the 13 per-shape means** (popcorn-cli service/mod.rs:1266): five 640×512 variants, four 60×1024 variants, plus 20×32, 40×176, 40×352, 8×2048 — so ~47 ms "median submission" on gpumode.com is a geomean, NOT a 640×512 time; a plain `torch.linalg.eigh` wrapper geomeans to ~59 ms (measured per-shape on B200).

Measured per-shape (B200 2026-07-14, ours = tridiag ps=16 + D&C leaf=128, no backtransform yet):
| shape | ours ms | torch.eigh ms |
|---|---|---|
| 20×32 | 0.262 | 0.151 |
| 40×176 | 2.675 | 5.761 |
| 40×352 | 9.011 | 18.474 |
| 640×512 | 94.6 | 192.9 |
| 60×1024 | 110.7 | 111.0 |
| 8×2048 | **454.2** | 172.6 |

(Table predates the leaf=32 D&C default: 640×512 is now 63.7 ms, 60×1024 102.6 — smaller shapes also gain but are unmeasured.) Geomeans over the 13 ranked shapes (512/1024 stress variants ≈ their plain shapes): ours ≈ 45 ms (≈ 48 with a ~5 ms backtransform allowance) vs torch ≈ 59 — i.e. right at the current leaderboard median even before optimization. Biggest levers, in log-weight order: (1) **8×2048 is latency-starved** (tridiag ≈ 400 of the 454 ms: batch 8 = 8 CTAs on 148 SMs × 128 sequential panel+rank2k rounds) — fixing to torch-parity alone is -7% geomean; a per-shape hybrid dispatch (use `torch.linalg.eigh` where it wins: 20×32, 8×2048) projects ≈ 41 ms; (2) the D&C's 50 ms at 640×512 is prep/leaf-bound (GEMMs ≈ 2 ms) — real headroom; (3) 1024-class tridiag (~84 ms) late-panel latency.

## D&C tridiagonal eigensolver (`tridiag_eig_dc_` in `eigh.py`)

Batched divide & conquer (LAPACK dstedc/dlaed0-4 chain) for step 2, fully on-device and CUDA-graph capturable — the deflation count K never reaches the host. Motivating profile (2026-07-14 nsys, `profiles/eigh_nsys/b640_n512_ps16_cublas_*`): raw cuSolverDx htev at N=512 silently fell to `Thread()` execution (one thread solves one 512×512 problem serially, 1 MB gmem eigenvector matrix per thread) and took **38 s** vs 43 ms tridiag. Block-mode htev with `Job<all_vectors>` is smem-resident by design: `4N² ≲ smem` caps it at **N ≤ ~240 on B200, ~157 on sm89** — `_load_htev_module` now **raises** on the thread-mode fallback instead of compiling the ~1000x-slower path.

Structure (one compile total for the prep module; htev compiles per leaf size):
- **Tree**: `_dc_tree(n, leaf)` halves until ≤ leaf (default 32, B200-tuned — see leaf sweep below; ≥3 required; ≤ 2-ish distinct leaf sizes). Tears subtract `|E[cut]|` from both straddling diagonals up front (dlaed0.f:263-268), rho read back from `E` per merge. No data-dependent natural splits — a negligible `E` at a tear just yields tiny z → full deflation, so the tree shape is uniform across the batch (graph-friendly).
- **Leaves**: batched block-mode htev over `B × leaves` subproblems per distinct size (gather D/E slices → one launch → scatter eigenvalues into `D`, vectors into the Z ping buffer's diagonal blocks).
- **Merge = one m×m `S_eff` + two bmm's.** The native `dc_prep_kernel` (plain-CUDA `load_inline` module `eigh_dc_prep_v1`, no fast-math — the secular iteration needs exact division; one CTA of 256 threads per (matrix, merge), grid `(G, B)`) folds *everything column-side* into `S_eff`: dlaed2 deflation (rule A tiny-z, rule B close-eigenvalues → Givens recorded then applied to S rows in reverse), the dlaed4 secular solve (one lane per root; **fixed-weight two-pole quadratic in the eta-step matched to (W, DW)** — dlaed4.f:633-658 interior, :324-340 last root — with Newton + bracket-midpoint safeguards, 40 fp32 iterations; value-only frozen-rest models 2-cycle on mid-cluster roots, and bisection-only loses relative accuracy near poles: both were observed failing before porting the LAPACK step), the Löwner ẑ recompute (dlaed3.f:260-290 — this is what keeps eigenvectors orthogonal under clustering), deflated pass-through unit columns, and the final ascending sort (column order of S_eff). Then per merge: `Z_out[top] = Q1 @ S[:n1, :]`, `Z_out[bot] = Q2 @ S[n1:, :]` — plain `torch.bmm` into strided `out=` views (verified no-copy), ping-ponged between the caller's `Z` and `ws.z_alt` (parity chosen so the last level lands in `Z`). **Unbalanced trees**: levels that don't cover all rows copy the pass-through segments' diagonal blocks across the ping buffers (missing this gave orth=1.0 at N=65 — balanced N=512/1000 hid it).
- Smem: `16·m` bytes/CTA (survivor poles, weights→ẑ, roots, origins); `m ≤ 4096` (attribute set at `prepare_dc`). Scratch: `fbuf (B,4,N)` fp32 + `ibuf (B,6,N)` int32, per-merge slices disjoint by construction. Workspace via `dc_workspace(batch, n, device, leaf_size)`; `warm_dc(n, leaf)` before graph capture.
- API: `tridiag_eig_dc_(D, E, Z, ws, *, leaf_size=32, queue_handle=0)` — `D` in/out (diag → ascending eigenvalues), `E` read-only, `Z (B,N,N)` gets eigenvector *columns*. fp32 throughout (matches slaed*; fp64 not needed — see gates).

Verified (2026-07-14, local sm89): fp64+fp32 Python mirror (scratchpad `test_dc.py`) vs `torch.linalg.eigh` on 6 stress families (random, diagonal, clustered/repeated, rank-deficient half-zero spectrum, graded 1e-12, zero-tear) at N ≤ 1000 — fp64 resid/orth ~1e-15, fp32 ~1e-6. Native (`test_dc_native.py`): same families, N ∈ {65, 128, 176, 256, 512, 1000}, leaf ∈ {16, 32, 128} — resid/orth ≤ 1e-5, eig ≤ 6e-6, all OK. Graph capture (`test_dc_graph.py`): replays bit-identical, equal to eager. Full pipeline (A → `tridiagonalize_` → D&C): spectrum vs `eigvalsh(A)` 3.9e-6 at 4×512 ps=16 locally; **B200 (2026-07-14, 640×512 ps=16 leaf=128, gates green): dc solve 50.0 ms, tridiag+dc 94.6 ms vs `torch.linalg.eigh` 192.9 ms (2.04x, and cuSOLVER's number includes the backtransform we don't do yet — but that's only ~172 GFLOP of GEMMs ≈ 4 ms)**; raw thread-mode htev was 38 s (760x). **60×1024 (gates green): dc 26.5 ms, tridiag+dc 110.7 ms vs `torch.linalg.eigh` 111.0 ms (1.00x)** — at N=1024 the tridiagonalization (~84 ms) dominates, not the D&C; tridiag perf work (smem-resident V/W, late-panel handling) is the lever there.

**nsys breakdown (B200 2026-07-14, 640×512 ps=16 leaf=128, `profiles/eigh_nsys/b640_n512_ps16_cublas_full_20260714T032556Z`)** — fine-grained NVTX ranges now live inside `tridiag_eig_dc_` (gated by `eigh.DC_NVTX`, default off so eval pays nothing; the runner's profile path flips it for the annotated launch). GPU time of the 50 ms dc_solve: **htev leaves 38.1 ms (76%)**, prep 9.5 (L0 m=256 3.1 + L1 m=512 6.4), bmm 2.1, gather/scatter/tears 0.4. Tridiag itself: 43.8 ms.
- **htev is one-warp-per-matrix by design**: `htev_block_kernel` launches with block_dim 32 (cuSolverDx's suggestion at batches-per-block 1) and 65 KB smem → ~3 warps/SM busy. `BlockDim<NT>` is a dead end — measured NT ∈ {32..256} bit-identical results and no speedup (extra warps idle; the BPB threshold table shows BPB>1 only for N≤15 fp32-with-vectors). NT stays plumbed (`EIGH_HTEV_NT` env, default 32, cache-keyed `_nt{nt}_v4`) for sweeps.
- **The leaf lever is leaf size**: htev work ≈ n·leaf² per matrix. Local sm89 sweep (64×512, dc solve, gates green at every point incl. N=65 leaf=16 unbalanced): leaf 128 → 59.8 ms, 64 → 19.1, **32 → 13.8**, 16 → 17.1 (deep-tree prep overtakes). **B200 confirmed (2026-07-14, gates green): 640×512 dc 50.0 → 24.8 (leaf=64) → 19.3 ms (leaf=32), tridiag+dc 94.6 → 63.7 ms (3.03x vs torch.eigh 192.9); 60×1024 dc 26.5 → 17.3, combined 110.7 → 102.6 (tridiag ~84 ms dominates there). leaf=32 is the new default choice at 512/1024.**
- **leaf=32 nsys re-split (B200 2026-07-14, `profiles/eigh_nsys/b640_n512_ps16_leaf32_*`)**: of the 19.4 ms dc_solve — htev 2.9, **prep 13.8 (71%: L0..L3 = 2.0/2.3/3.1/6.4)**, bmm 2.5, rest 0.2. Prep is the whole game now.
- **Prep phase attribution** (in-kernel `clock64()` timers: build with `EIGH_DC_TIMERS=1` → module `eigh_dc_prep_v2_timers`, CTA (0,0) printfs per-phase cycles; DC_PHASE_MARK macros in the kernel). On *real* tridiag input (little deflation, k≈m; local sm89, m=512, k=441): **Phase E Löwner 44%**, **Phase D secular 27%** (after the 8·eps early-exit — see below), Phase C serial deflation scan 12%, F1-F4+A/B ≈ 17%. On heavy-deflation input (raw random D/E, k=24) the profile inverts: C/F1/F2 dominate — stress-family perf will differ from random-case perf. Suspected driver of E's (and D's) high per-term cost: fp32 `div.rn` slow-path hits on tiny pole gaps (~1.6k cycles/term measured in E); unverified, and may differ on B200.
- **Secular early-exit shipped (2026-07-14)**: per-lane break when `|eta| <= 8eps*|tau|` (1·eps is unreachable in fp32 sum noise — measured no benefit; 8·eps cut Phase D 3.6M → 0.84M cycles). A hard cap of 10 iters *fails* the eig gate (4.9e-3): slow-converging roots are real, 40 stays as the safety cap. Also: Phase F1 zeroing rewritten row-major (no per-element int64 div/mod). Net local: dc 13.8 → **9.3 ms** (64×512). All stress families + graph capture re-verified green (clustered orth ~5e-7 unchanged — the 8·eps break does not degrade the sensitive families). **B200 confirmed (2026-07-14, gates green, run includes the stage=6 matvec prefetch change): 640×512 dc 19.3 → 12.7 ms, pipeline 62.7 (3.1x vs torch.eigh); 60×1024 dc 17.3 → 10.3, pipeline 87.6 (1.27x).** Implied tridiag split from the same run: 1024-class ~77 ms (stage=6 helped, was ~85); 512-class ~50 ms vs ~44 before — possible stage=6 regression at N=512 (smem ring vs CTA residency), A/B stage=2 vs 6 at 640×512 on B200 before locking the default. Next levers, in order: Phase E/D per-term cost (scaled Löwner accumulation to dodge div slow paths; dlaed4-style initial guess to cut iterations), Phase C staging dw/zv in smem + merge-path parallel sort, B200 re-bench + re-profile. NOT GEMM precision — the merge GEMMs are ~2.5 ms total.

## CuTe DSL performance rules (measured in this project)

- The DSL is a **tracing JIT**: Python runs at compile time; layouts/partitions/`local_tile`/`domain_offset` with `const_expr` args cost **nothing at runtime**. Dynamic *values* (predicates, limits, pointer offsets) compile to a few integer ops — cheap, like hand-written CUDA indexing.
- **Never respecialize per loop iteration** (`cutlass.range_constexpr` + per-i `const_expr` tile sizes): it duplicates the kernel body per iteration — measured 22x compile time and up to 1.85x runtime regression (I-cache) at panel_size=32, while ceil-rounding made the "smaller" tiles identical anyway. Use `cutlass.range(..., unroll_full=True)` with one traced body + dynamic predicates instead.
- **Keep predicate limits static when possible**: with a static limit, most of `predicate_k`'s per-thread booleans constant-fold away. Making the bound depend on runtime `i` turned them into live registers and measured 3–11% *slower* — for <2% traffic saved. Columns `< i` are already nullified by `v`'s zeros.
- `cp.async` needs ≥4-byte transactions: `num_copy_elems=1` copies only work for fp32.
- Smem budget: `sCol + sB + K*rows_per_tile*tiler_n` elements (V/W are gmem; `K = stage`, the matvec pipeline depth). At N=512 each `sA` stage is 8 KB, so K=6 ≈ 52 KB; at N=4096 each stage is 32 KB, so K=6 ≈ 224 KB (fits B200's 227 KB but is the practical ceiling there). Exceeding the arch limit fails at **launch**, not compile ("launch shared memory exceeds current GPU arch"), with the allocated byte count in the message (local sm_89 ~99 KB ceiling ⇒ K≤11 at N=512, K≤2 at N=4096). Higher K also lowers CTAs/SM (occupancy), which on B200 is the actual limiter — see step 4; the default `stage=2` is deliberately low, not smem-driven.
- Don't add synchronization unless needed for correctness; document why each barrier exists.
- Use `cutlass.Constexpr` / `const_expr` type annotations aggressively so the compiler sees static structure.
- The DSL warns at ≥64 iterations of a static loop; treat that as a hard smell.
- **Never use a ternary expression with a dynamic condition** (`x if dyn_cond else y`): the condition is resolved at trace time and one branch is silently baked in (trace-time-arbitrary — can even differ between compiles). Use a dynamic `if` statement, which the preprocessor lowers correctly. Cost of a masked scalar update this way is negligible.

## Verify & bench

```bash
# correctness + reflector debug output
.venv/bin/python eigh_bench_local_rtx4050.py --batch 2 --n 512

# benchmark (CUDA graphs + L2 rotation) with torch w-column baseline + allclose gate
.venv/bin/python eigh_bench_local_rtx4050.py --batch 640 --n 512 --bench --skip-expected --bench-repeats 3

# full tridiagonalization: D/E gate + graph-captured driver benches (small batch/calls locally)
.venv/bin/python eigh_bench_local_rtx4050.py --batch 8 --n 512 --panel-size 16 --tri --bench --skip-expected --bench-calls 8 --bench-sets 2

# full pipeline (tridiag + D&C eigensolve): gates + graph-captured benches.
# Local htev leaf builds need MATHDX_ROOT (Modal bakes it into the image):
MATHDX_ROOT=$PWD/nvidia-mathdx-26.06.0-cuda13/nvidia/mathdx/26.06 \
  .venv/bin/python eigh_bench_local_rtx4050.py --batch 4 --n 512 --panel-size 16 \
  --tri-solve --skip-expected --bench --bench-calls 4 --bench-sets 2
```

When verifying kernel changes, always cover: an odd N (predicates), a `warps_per_row=2` config (`block_sum` path + reduction barriers — use the `threads_per_row=64` override at N=512, since N=4096 no longer fits local smem with V/W panels), `panel_size > 1` (correction GEMVs + smem-reuse barriers across `i`), and a **tail-panel N** for the tridiag driver (`(N-1) % ps != 0`, e.g. N=67 ps=8). Compare against float64 torch references mirroring the kernel's exact recurrence — per-panel w (see the scratchpad `test_panel.py` mirror) and full-factorization D/E (`torch_tridiag_reference`, exercised by the scratchpad `test_tridiag.py`).

For D&C changes, always cover: an **unbalanced tree** (a level with pass-through segments, e.g. N=65 leaf=16 — the config that caught the ping-pong staleness bug), a deep tree (small leaf), the stress families (clustered/repeated eigenvalues exercise rule-B Givens deflation; diagonal exercises the K=0 full-deflation path; rank-deficient exercises mid-cluster secular roots), and graph capture. Scratchpad suites: `test_dc.py` (Python mirror, the formula reference), `test_dc_native.py`, `test_dc_graph.py`.
